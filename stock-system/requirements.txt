# ── Core async I/O ──────────────────────────────────────────────────────────
httpx>=0.27.0          # async HTTP client (NewsAPI calls)
aiosqlite>=0.20.0      # async SQLite access

# ── Market data ─────────────────────────────────────────────────────────────
yfinance>=0.2.40       # OHLCV + fundamental data (wraps Yahoo Finance)

# ── Configuration ───────────────────────────────────────────────────────────
python-dotenv>=1.0.0   # .env loading

# ── LLM backends (orchestrator.py) ──────────────────────────────────────────
anthropic>=0.28.0      # Claude API (LLM_BACKEND=claude)
openai>=1.30.0         # vLLM OpenAI-compatible client (LLM_BACKEND=vllm)

# ── Sentiment analysis (sentiment.py — FinBERT) ──────────────────────────────
transformers>=4.41.0
torch>=2.3.0           # CPU-only install: pip install torch --index-url https://download.pytorch.org/whl/cpu

# ── Forecasting (forecasting.py — Chronos-T5) ────────────────────────────────
# git+https://github.com/amazon-science/chronos-forecasting.git
# Uncomment the line above once you are ready to use Chronos-T5.
# It pulls in additional torch / huggingface dependencies.
